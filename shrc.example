#!/bin/sh

nag() { en "${1%_*}" sa-ssh -v "${1##*_}"; }
converge() { sa-ssh "$@" sudo chef-client; }

bgrep() { git branch -a | grep "$*" | sed 's,remotes/,,'; }
breset() { git update-ref "refs/heads/$1" "origin/$1"; }
